<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.eng.courses.android.lesson3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".recyclerview.RecyclerViewActivity" />
        <activity
            android:name=".views.ViewsActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".layouts.LayoutsActivity" />
        <activity android:name=".layouts.FrameActivity" android:theme="@style/AppThemeWithActionBar"/>
        <activity android:name=".layouts.LinearActivity"  android:theme="@style/AppThemeWithActionBar"/>
        <activity android:name=".layouts.RelativeActivity" android:theme="@style/AppThemeWithActionBar"/>
    </application>

</manifest>